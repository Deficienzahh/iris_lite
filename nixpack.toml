[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["npm install --prefix frontend", "npm run build --prefix frontend"]

[start]
cmd = "python backend/app.py"